
struct ButtonColors := {
    base: color,
    pressed: color,
    hovered: color,
}

export global DefaultStyle := {
  property <color> background: #222;
  property <color> disabled: #99acb0;
  property <color> accent: #3d5a73;
  property <color> highlight: #286b43;
  property <color> highlightDarker: #25633e;
}